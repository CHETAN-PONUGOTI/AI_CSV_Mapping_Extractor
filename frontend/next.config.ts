/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you ever need to bypass CORS during local dev without Docker, you can add rewrites here
};

export default nextConfig;